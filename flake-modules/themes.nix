# Themes module for flake-parts
# This module provides theme configuration options that can be used in settings.nix

{ ... }: {
  # This module defines theme options that are consumed by settings.nix
  # The actual theme configurations are applied in host configurations
  # based on the theme selections in settings.nix
}
