# Hardware module for flake-parts
# This module provides hardware configuration options that can be used in settings.nix

{ ... }: {
  # This module defines hardware profile options that are consumed by settings.nix
  # The actual hardware configurations are applied in host configurations
  # based on the hardware profile selection in settings.nix
}
